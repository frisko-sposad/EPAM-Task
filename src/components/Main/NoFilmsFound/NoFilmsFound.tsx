import React, { FC } from 'react';
import { Container, MainContainer } from '../Main.styled';

const NoFilmsFound: FC = () => (
  <MainContainer>
    <Container>No Films Found</Container>
  </MainContainer>
);

export default NoFilmsFound;
