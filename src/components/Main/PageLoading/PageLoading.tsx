import React, { FC } from 'react';
import { Container, MainContainer } from '../Main.styled';

const PageLoading: FC = () => (
  <MainContainer>
    <Container>Loading...</Container>
  </MainContainer>
);

export default PageLoading;
